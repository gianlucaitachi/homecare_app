name: homecare_backend
description: A server-side application for the HomeCare project.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  postgres: ^2.6.1
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  dart_jsonwebtoken: ^2.12.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
